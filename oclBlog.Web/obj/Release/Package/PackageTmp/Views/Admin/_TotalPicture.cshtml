@model IEnumerable<ocLBlog.Entities.Models.Article>
@{
    Layout = null;
}

@foreach (var item in Model)
{
    <div class="col-lg-6">
        <div class="panel">
            <div class="panel-body">
                <div class="bio-chart">
                    <input class="knob" data-width="100" data-height="100" data-displayPrevious=true data-thickness=".2" value="@item.Pictures.Count()" data-fgColor="#4CC5CD" data-bgColor="#e8e8e8">
                </div>
                <div class="bio-desk">
                    <h4 class="terques">Makale Resmi </h4>
                    <p>Toplam : @item.Pictures.Count()</p>
                    <p>Son Eklenme : @item.CreatedDate.ToShortDateString()</p>
                </div>
            </div>
        </div>
    </div>
}
