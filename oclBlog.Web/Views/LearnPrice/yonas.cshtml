@using PagedList
@using PagedList.Mvc
@model IPagedList<ocLBlog.Entities.Models.LearnPrice>
@{
    ViewBag.Title = "Gelen Fiyat Teklifleri";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<section id="container" class="">
    <section id="main-content">
        <section class="wrapper">
            <div class="row">
                <div class="col-lg-12">
                    <section class="panel">
                        <header class="panel-heading">
                            @ViewBag.Title ||
                        </header>
                        <table class="table table-striped table-advance table-hover">
                            <thead>
                                <tr>
                                    <th>Adı Soyadı</th>
                                    <th>Mail Adresi</th>
                                    <th>Telefon No:</th>
                                    <th>Şehir / Ülke</th>
                                    <th>Eklenme Tarihi</th>
                                    <th>İşlemler</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td>@item.YourNameSurname</td>
                                        <td>@item.YourMailAddress</td>
                                        <td>@item.YourPhoneNumber</td>
                                        <td>@item.YourCityandCountry</td>
                                        <td>@item.CreatedDate.ToShortDateString()</td>
                                        <td>
                                            <a href="~/LearnPrice/LearnPriceDetail/@item.Id"><button class="btn btn-info btn-xs"><i class="icon-trash ">Detaya Bak</i></button></a>
                                            <a href="~/LearnPrice/Delete/@item.Id"><button class="btn btn-danger btn-xs"><i class="icon-trash ">Sil</i></button></a>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </section>
                </div>
            </div>
            <div class="pagination" align="center">
                <div class="pagination-large">
                    @Html.PagedListPager(Model, Page => Url.Action("yonas", new { Page }), PagedListRenderOptions.ClassicPlusFirstAndLast)
                </div>
            </div>
        </section>
    </section>
</section>