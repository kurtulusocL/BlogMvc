@using PagedList
@using PagedList.Mvc
@model IPagedList<ocLBlog.Entities.Models.Portfolio>
@{
    ViewBag.Title = "Çalışmalarım";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<section id="container" class="">
    <section id="main-content">
        <section class="wrapper">
            <div class="row">
                <div class="col-lg-12">
                    <section class="panel">
                        <header class="panel-heading">
                            @ViewBag.Title || <a href="~/Portfolio/Create"><button class="btn btn-primary">Yeni Porfolyö Ekle</button></a>
                        </header>
                        <table class="table table-striped table-advance table-hover">
                            <thead>
                                <tr>
                                    <th width="90">Resim</th>
                                    <th>Başlık</th>
                                    <th>Altbaşlık</th>
                                    <th>Eklenme Tarihi</th>
                                    <th>İşlemler</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td><img src="~/img/port/@item.Photo" class="img-responsive"/></td>
                                        <td>@item.Title</td>
                                        <td>@item.Subtitle</td>
                                        <td>@item.CreatedDate.ToShortDateString()</td>
                                        <td>                                           
                                            <a href="~/Portfolio/Edit/@item.Id"><button class="btn btn-success btn-xs"><i class="icon-trash ">Güncelle</i></button></a>
                                            <a href="~/Portfolio/Delete/@item.Id"><button class="btn btn-danger btn-xs"><i class="icon-trash ">Sil</i></button></a>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </section>
                </div>
            </div>
            <div class="pagination" align="center">
                <div class="pagination-large">
                    @Html.PagedListPager(Model, Page => Url.Action("yonas", new { Page }), PagedListRenderOptions.ClassicPlusFirstAndLast)
                </div>
            </div>
        </section>
    </section>
</section>
