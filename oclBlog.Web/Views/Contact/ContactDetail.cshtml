@model ocLBlog.Entities.Models.Contact
@{
    ViewBag.Title = "İletişimden Gelen Mesaj Ayrıntısı";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<section id="container" class="">
    <section id="main-content">
        <section class="wrapper">
            <div class="row">
                <div class="col-sm-10">
                    <section class="panel">
                        <header class="panel-heading">
                            @ViewBag.Title || <a href="~/Contact/yonas" class="btn btn-primary">İletişim Admin Sayfasına Dön</a> || <a href="~/Contact/Delete/@Model.Id" class="btn btn-danger">Mesajı Sil</a>
                        </header>
                        <table class="table table-striped">
                            <thead>
                                <tr>
                                    <th>#</th>
                                    <th>Adı-Soyadı</th>
                                    <th>Mesaj Konusu</th>
                                    <th>Mesaj</th>
                                </tr>
                            </thead>
                            <tbody>
                                <tr>
                                    <td>1</td>
                                    <td>@Model.NameSurname</td>
                                    <td>@Model.Subject</td>
                                    <td>@Model.Text</td>
                                </tr>
                            </tbody>
                        </table>
                    </section>
                </div>
            </div>
        </section>
    </section>
</section>